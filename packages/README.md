# Packages (upstream)

| Package | Role |
|---------|------|
| `medical_shared` | Shared Dart/Flutter code consumed by downstream tracks (e.g. SRS V2). |

Add new packages as folders with their own `pubspec.yaml`. Downstream projects reference them via `path:` relative to this repo.
