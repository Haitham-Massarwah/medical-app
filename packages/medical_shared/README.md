# medical_shared

Shared code for **Medical Appointments**. Lives under upstream repo `medical-app`.

- Add new shared models, API clients, and utilities **here** (not duplicated in downstream app folders).
- Downstream **Medical Appointments V2** references this package with a `path` dependency.

See `Medical Appointments V2/docs/UPSTREAM_WORKFLOW.md` for merge and SRS rules.
