name: medical_shared
description: Shared Dart/Flutter library for Medical Appointments. Source of truth is the medical-app repo; V2 consumes via path dependency.
version: 0.2.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
